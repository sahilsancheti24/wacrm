# TODO — Instagram webhook follow-up cleanup

## Steps
- [x] 1. Document `INSTAGRAM_APP_SECRET` in `.env.local.example`
- [x] 2. Make `scripts/test-instagram-webhook.mjs` prefer `INSTAGRAM_APP_SECRET` (fall back to `META_APP_SECRET`)
- [x] 3. Add `dev-server.log` and `messages/*.bak` to `.gitignore`
- [x] 4. Commit + push `TODO.md` update + cleanup changes to `main`

